import { Story, Meta } from '@storybook/react'
import CartSlider from '.'

export default {
  title: 'CartSlider',
  component: CartSlider
} as Meta

export const Default: Story = () => <CartSlider />
